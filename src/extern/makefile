
#Detect architecture
ARCH=$(shell uname)


obj:
#	@touch obj
#	ar t $(ARCH)/libgd.a    | xargs -n1 -i{} echo "ar x $(ARCH)/libgd.a    {}; mv {} gd_{}   " |sh
#	ar t $(ARCH)/libvogle.a | xargs -n1 -i{} echo "ar x $(ARCH)/libvogle.a {}; mv {} vogle_{}" |sh
#	cp $(ARCH)/*.o
		

clean:
#	@echo *.o 
#	@rm -f *.o
#	@rm -f obj
